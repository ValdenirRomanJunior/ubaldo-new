import { colors } from '../colors';
import { fonts } from '../fonts';

export const theme = {
    colors:{
        primary: colors.PRYMARY,
        border_primary:colors.BORDER_PRIMARY,
        button: colors.BUTTON,
        card_buttons:colors.CARD_BUTTONS,
        color_title:colors.COLOR_TITLE,
        color_subtitle:colors.COLOR_SUBTITLE,
        color_comum_text:colors.COLOR_COMUM_TEXT,
        color_link_menu:colors.COLOR_LINK_MENU,
        background:"#FFFFFF",
        red: colors.RED,
        green: colors.GREEN
    },

    fonts:{
        font_primary:fonts.FONT_PRIMARY,
        font_secondary:fonts.FONT_SECONDARY
    }
}